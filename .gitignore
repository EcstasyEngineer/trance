.vs
Win32
x64
build/
*.vcxproj.user
*.opensdf
*.opendb
*.db

# In-source CMake artifacts (real builds live in build/). These names are always
# generated and never collide with the tracked cmake/Find*.cmake modules.
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
compile_commands.json

# Headless v2 build artifacts + machine-specific build scripts (paths are local).
obj/
*.exe
build_golden.bat
build_v2_demo.bat
